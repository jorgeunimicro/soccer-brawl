import { GetGroupNamePipe } from './get-group-name.pipe';

describe('GetGroupNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetGroupNamePipe();
    expect(pipe).toBeTruthy();
  });
});
